package slotegrator.project.dictionaries;

public enum SortPlayersEnum {
    USERNAME,
    EXTERNAL_ID,
    NAME,
    LAST_NAME,
    E_MAIL,
    PHONE,
    HALL,
    REGISTRATION_DATE,
    LAST_VISIT,
    VERIFIED_PLAYER,
    STATUS
}
